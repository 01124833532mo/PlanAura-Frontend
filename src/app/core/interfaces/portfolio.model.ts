/** Mirrors Planura.Core.Application.Models.Vendor.PortfolioMediaItemDto exactly. */
export interface PortfolioMediaItem {
  id: number;
  fileUrl: string;
  title: string | null;
  displayOrder: number;
}
